---
phase: 03-verification-dashboard
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - spectrace/spectrace/settings.py
  - spectrace/requirements/admin.py
  - spectrace/requirements/dashboard.py
  - spectrace/templates/admin/index.html
autonomous: true

must_haves:
  truths:
    - "Dashboard shows all requirements organized by hierarchy"
    - "Dashboard shows summary metrics: total requirements, % passing, % failing, % untested"
    - "Untested requirements are visually highlighted with yellow/orange background"
    - "Status indicators use color dots: green (pass), red (fail), gray (untested)"
  artifacts:
    - path: "spectrace/spectrace/settings.py"
      provides: "django-unfold configuration"
      contains: "UNFOLD"
    - path: "spectrace/requirements/admin.py"
      provides: "Unfold-styled ModelAdmin for Requirement, TestRun, TestResult"
      contains: "unfold.admin"
    - path: "spectrace/requirements/dashboard.py"
      provides: "Dashboard callback with metrics and tree data"
      contains: "dashboard_callback"
    - path: "spectrace/templates/admin/index.html"
      provides: "Custom dashboard template with tree view and metrics banner"
      contains: "requirements_tree"
  key_links:
    - from: "settings.py"
      to: "dashboard.py"
      via: "DASHBOARD_CALLBACK setting"
      pattern: "requirements.dashboard.dashboard_callback"
    - from: "dashboard.py"
      to: "models.py"
      via: "Requirement.objects queries for metrics"
      pattern: "Requirement.objects"
    - from: "index.html"
      to: "dashboard.py"
      via: "context variables from callback"
      pattern: "total_requirements|passing_pct|requirements_tree"
---

<objective>
Build the Django dashboard with django-unfold showing requirements hierarchy, summary metrics, and coverage gap highlighting.

Purpose: Provide PMs, engineers, and QA with a visual interface to see which requirements are verified, failing, or need test coverage.

Output: Admin dashboard with summary banner (total, % passing, % failing, % untested), hierarchical tree view of requirements with status indicators, and visual highlighting of untested requirements (yellow/orange background).
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-verification-dashboard/03-CONTEXT.md
@.planning/phases/03-verification-dashboard/03-RESEARCH.md
@spectrace/spectrace/settings.py
@spectrace/requirements/models.py
@spectrace/requirements/admin.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Configure django-unfold and update admin registrations</name>
  <files>
    spectrace/spectrace/settings.py
    spectrace/requirements/admin.py
  </files>
  <action>
1. Update spectrace/spectrace/settings.py:
   - Add "unfold" BEFORE "django.contrib.admin" in INSTALLED_APPS:
     ```python
     INSTALLED_APPS = [
         "unfold",  # Must be before django.contrib.admin
         "unfold.contrib.filters",  # Enhanced filters
         'django.contrib.admin',
         ...
     ]
     ```
   - Add UNFOLD settings:
     ```python
     UNFOLD = {
         "SITE_TITLE": "SpecTrace",
         "SITE_HEADER": "SpecTrace Dashboard",
         "DASHBOARD_CALLBACK": "requirements.dashboard.dashboard_callback",
     }
     ```
   - Update TEMPLATES to include project templates directory:
     ```python
     TEMPLATES = [
         {
             'BACKEND': 'django.template.backends.django.DjangoTemplates',
             'DIRS': [BASE_DIR / "templates"],  # Add this
             ...
         },
     ]
     ```

2. Update spectrace/requirements/admin.py:
   - Import from unfold.admin instead of django.contrib.admin
   - Update Requirement admin with verification_status display
   - Register TestRun and TestResult with unfold.admin.ModelAdmin
   ```python
   from django.contrib import admin
   from unfold.admin import ModelAdmin
   from .models import Requirement, TestRun, TestResult

   @admin.register(Requirement)
   class RequirementAdmin(ModelAdmin):
       list_display = ['external_id', 'title', 'verification_status', 'priority', 'status', 'updated_at']
       list_filter = ['verification_status', 'status', 'priority']
       search_fields = ['external_id', 'title', 'description']
       readonly_fields = ['verification_status', 'created_at', 'updated_at']

   @admin.register(TestRun)
   class TestRunAdmin(ModelAdmin):
       list_display = ['source_file', 'imported_at', 'total_tests', 'passed', 'failed', 'errors', 'skipped']
       readonly_fields = ['imported_at']
       ordering = ['-imported_at']

   @admin.register(TestResult)
   class TestResultAdmin(ModelAdmin):
       list_display = ['test_nodeid', 'status', 'test_run', 'time']
       list_filter = ['status', 'test_run']
       search_fields = ['test_nodeid', 'name', 'classname']
   ```
  </action>
  <verify>
    cd /Users/tslater/dev/spec-trace/spectrace && python -c "
from django.conf import settings
print('UNFOLD in settings:', 'UNFOLD' in dir(settings))
print('unfold in INSTALLED_APPS:', 'unfold' in settings.INSTALLED_APPS)
from requirements.admin import RequirementAdmin, TestRunAdmin, TestResultAdmin
print('Admins imported successfully')
"
  </verify>
  <done>
    - unfold first in INSTALLED_APPS
    - UNFOLD config with SITE_TITLE, SITE_HEADER, DASHBOARD_CALLBACK
    - TEMPLATES includes project templates dir
    - RequirementAdmin shows verification_status
    - TestRun and TestResult registered with unfold.admin.ModelAdmin
  </done>
</task>

<task type="auto">
  <name>Task 2: Create dashboard callback and custom template</name>
  <files>
    spectrace/requirements/dashboard.py
    spectrace/templates/admin/index.html
  </files>
  <action>
1. Create spectrace/requirements/dashboard.py:
   ```python
   """Dashboard callback for SpecTrace metrics and tree data."""
   from django.db.models import Count, Q
   from .models import Requirement

   def dashboard_callback(request, context):
       """Provide dashboard metrics for the admin index page.

       Returns context with:
       - total_requirements, passing_count, failing_count, untested_count
       - passing_pct, failing_pct, untested_pct
       - requirements_tree (annotated list for hierarchical display)
       """
       total = Requirement.objects.count()

       if total > 0:
           metrics = Requirement.objects.aggregate(
               passing=Count('id', filter=Q(verification_status='passing')),
               failing=Count('id', filter=Q(verification_status='failing')),
               untested=Count('id', filter=Q(verification_status='untested')),
           )

           context.update({
               'total_requirements': total,
               'passing_count': metrics['passing'],
               'failing_count': metrics['failing'],
               'untested_count': metrics['untested'],
               'passing_pct': round(metrics['passing'] * 100 / total, 1),
               'failing_pct': round(metrics['failing'] * 100 / total, 1),
               'untested_pct': round(metrics['untested'] * 100 / total, 1),
           })
       else:
           context.update({
               'total_requirements': 0,
               'passing_count': 0,
               'failing_count': 0,
               'untested_count': 0,
               'passing_pct': 0,
               'failing_pct': 0,
               'untested_pct': 0,
           })

       # Get requirements tree for hierarchical display
       # get_annotated_list returns [(node, info), ...] where info has 'open', 'close', 'level'
       context['requirements_tree'] = Requirement.get_annotated_list()

       return context
   ```

2. Create spectrace/templates/admin/index.html directory and template:
   ```bash
   mkdir -p spectrace/templates/admin
   ```

   Create the template that extends unfold's base:
   ```html
   {% extends "unfold/layouts/base.html" %}
   {% load i18n %}

   {% block content %}
   <div class="p-6">
       <!-- Metrics Banner -->
       <div class="mb-8 grid grid-cols-1 md:grid-cols-4 gap-4">
           <div class="bg-white dark:bg-gray-800 rounded-lg shadow p-4">
               <div class="text-sm text-gray-500 dark:text-gray-400">Total Requirements</div>
               <div class="text-3xl font-bold text-gray-900 dark:text-white">{{ total_requirements }}</div>
           </div>
           <div class="bg-white dark:bg-gray-800 rounded-lg shadow p-4 border-l-4 border-green-500">
               <div class="text-sm text-gray-500 dark:text-gray-400">Passing</div>
               <div class="text-3xl font-bold text-green-600">{{ passing_count }}</div>
               <div class="text-sm text-green-500">{{ passing_pct }}%</div>
           </div>
           <div class="bg-white dark:bg-gray-800 rounded-lg shadow p-4 border-l-4 border-red-500">
               <div class="text-sm text-gray-500 dark:text-gray-400">Failing</div>
               <div class="text-3xl font-bold text-red-600">{{ failing_count }}</div>
               <div class="text-sm text-red-500">{{ failing_pct }}%</div>
           </div>
           <div class="bg-white dark:bg-gray-800 rounded-lg shadow p-4 border-l-4 border-yellow-500">
               <div class="text-sm text-gray-500 dark:text-gray-400">Untested</div>
               <div class="text-3xl font-bold text-yellow-600">{{ untested_count }}</div>
               <div class="text-sm text-yellow-500">{{ untested_pct }}%</div>
           </div>
       </div>

       <!-- Requirements Tree -->
       <div class="bg-white dark:bg-gray-800 rounded-lg shadow">
           <div class="p-4 border-b border-gray-200 dark:border-gray-700">
               <h2 class="text-lg font-semibold text-gray-900 dark:text-white">Requirements</h2>
           </div>
           <div class="p-4">
               {% if requirements_tree %}
               <ul class="space-y-1">
                   {% for node, info in requirements_tree %}
                   {% if info.open %}
                   <li style="margin-left: {{ info.level|add:info.level }}rem;">
                       <div class="flex items-center p-2 rounded-lg {% if node.verification_status == 'untested' %}bg-yellow-50 dark:bg-yellow-900/20{% endif %}">
                           <!-- Status dot -->
                           <span class="w-3 h-3 rounded-full mr-3
                               {% if node.verification_status == 'passing' %}bg-green-500
                               {% elif node.verification_status == 'failing' %}bg-red-500
                               {% else %}bg-gray-400{% endif %}">
                           </span>
                           <!-- Requirement info -->
                           <a href="{% url 'admin:requirements_requirement_change' node.pk %}"
                              class="flex-1 hover:underline">
                               <span class="font-mono text-sm text-gray-600 dark:text-gray-400">{{ node.external_id }}</span>
                               <span class="ml-2 text-gray-900 dark:text-white">{{ node.title }}</span>
                           </a>
                           <!-- Status badge -->
                           <span class="text-xs px-2 py-1 rounded
                               {% if node.verification_status == 'passing' %}bg-green-100 text-green-800 dark:bg-green-900 dark:text-green-200
                               {% elif node.verification_status == 'failing' %}bg-red-100 text-red-800 dark:bg-red-900 dark:text-red-200
                               {% else %}bg-yellow-100 text-yellow-800 dark:bg-yellow-900 dark:text-yellow-200{% endif %}">
                               {{ node.verification_status|title }}
                           </span>
                       </div>
                   {% endif %}
                   {% for close in info.close %}</li>{% endfor %}
                   {% endfor %}
               </ul>
               {% else %}
               <p class="text-gray-500 dark:text-gray-400">No requirements found. Run <code>python manage.py parse_specs</code> to import specs.</p>
               {% endif %}
           </div>
       </div>

       <!-- Quick Actions -->
       <div class="mt-6 flex gap-4">
           <a href="{% url 'admin:requirements_requirement_changelist' %}"
              class="px-4 py-2 bg-blue-600 text-white rounded-lg hover:bg-blue-700">
               View All Requirements
           </a>
           <a href="{% url 'admin:requirements_testrun_changelist' %}"
              class="px-4 py-2 bg-gray-600 text-white rounded-lg hover:bg-gray-700">
               View Test Runs
           </a>
       </div>
   </div>
   {% endblock %}
   ```

Note: The template uses Tailwind CSS classes which django-unfold includes. The status highlighting uses:
- Green dot + green badge for passing
- Red dot + red badge for failing
- Gray dot + yellow badge + yellow background row for untested (makes coverage gaps obvious)
  </action>
  <verify>
    cd /Users/tslater/dev/spec-trace/spectrace && python -c "
from requirements.dashboard import dashboard_callback
print('Dashboard callback imported successfully')
" && ls -la templates/admin/index.html
  </verify>
  <done>
    - dashboard.py exists with dashboard_callback function
    - templates/admin/index.html exists with metrics banner and tree view
    - Untested requirements have yellow/orange highlighting
    - Status dots are green/red/gray
  </done>
</task>

<task type="auto">
  <name>Task 3: Verify full workflow and dashboard display</name>
  <files></files>
  <action>
1. Ensure superuser exists (create if needed):
   ```bash
   cd /Users/tslater/dev/spec-trace/spectrace
   python manage.py createsuperuser --noinput --username admin --email admin@test.com || true
   # If already exists, that's fine
   ```

2. Parse specs if not already done:
   ```bash
   python manage.py parse_specs specs/
   ```

3. Run tests and generate JUnit XML:
   ```bash
   pytest tests/ --junitxml=test_results.xml -o junit_family=xunit2
   ```

4. Extract links and import results:
   ```bash
   python manage.py extract_links --output links.json
   python manage.py import_results test_results.xml --links links.json
   ```

5. Start development server and verify dashboard:
   ```bash
   python manage.py runserver
   # Visit http://localhost:8000/admin/
   # Login with admin credentials
   # Verify:
   # - Metrics banner shows total, passing, failing, untested counts
   # - Requirements tree displays hierarchically
   # - Status dots are colored (green/red/gray)
   # - Untested requirements have yellow background
   ```

6. Document the dashboard URL and any visual observations.
  </action>
  <verify>
    cd /Users/tslater/dev/spec-trace/spectrace && python -c "
import django
django.setup()
from requirements.models import Requirement
from requirements.dashboard import dashboard_callback

# Simulate dashboard context
class MockRequest:
    pass

context = {}
result = dashboard_callback(MockRequest(), context)
print(f'Total requirements: {result.get(\"total_requirements\", 0)}')
print(f'Passing: {result.get(\"passing_count\", 0)} ({result.get(\"passing_pct\", 0)}%)')
print(f'Failing: {result.get(\"failing_count\", 0)} ({result.get(\"failing_pct\", 0)}%)')
print(f'Untested: {result.get(\"untested_count\", 0)} ({result.get(\"untested_pct\", 0)}%)')
print(f'Tree items: {len(result.get(\"requirements_tree\", []))}')
print('Dashboard callback works correctly')
"
  </verify>
  <done>
    - Superuser exists for admin access
    - Full workflow runs: parse_specs -> pytest --junitxml -> extract_links -> import_results
    - Dashboard accessible at /admin/
    - Metrics banner displays correctly
    - Requirements tree shows hierarchy with status indicators
    - Untested requirements visually highlighted
  </done>
</task>

</tasks>

<verification>
After all tasks complete:

1. Admin site styled with Unfold:
   ```bash
   cd spectrace && python manage.py check
   ```

2. Dashboard callback provides metrics:
   ```bash
   cd spectrace && python -c "
import django
django.setup()
from requirements.dashboard import dashboard_callback
context = {}
result = dashboard_callback(None, context)
assert 'total_requirements' in result
assert 'requirements_tree' in result
print('Dashboard callback works')
"
   ```

3. Visual verification (manual):
   - Start server: `python manage.py runserver`
   - Visit http://localhost:8000/admin/
   - Confirm metrics banner shows counts and percentages
   - Confirm requirements tree shows hierarchy
   - Confirm status dots are colored
   - Confirm untested rows have yellow background
</verification>

<success_criteria>
- Admin interface uses django-unfold theme (modern Tailwind UI)
- Dashboard index shows metrics banner: total requirements, passing %, failing %, untested %
- Requirements displayed in hierarchical tree view
- Status indicators: green dot (passing), red dot (failing), gray dot (untested)
- Untested requirements have yellow/orange background highlighting (coverage gaps visible at a glance)
- Quick action links to view all requirements and test runs
</success_criteria>

<output>
After completion, create `.planning/phases/03-verification-dashboard/03-02-SUMMARY.md`
</output>
