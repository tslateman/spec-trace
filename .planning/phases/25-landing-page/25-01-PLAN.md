---
phase: 25-landing-page
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - spectrace/templates/admin/requirements/landing.html
autonomous: false

must_haves:
  truths:
    - "Visitor reads one-line value proposition and understands SpecTrace's purpose"
    - "Visitor sees 4 feature highlights with icons explaining capabilities"
    - "Visitor can click feature highlights to navigate to relevant views"
    - "Landing page renders correctly in dark mode"
  artifacts:
    - path: "spectrace/templates/admin/requirements/landing.html"
      provides: "Enhanced landing page with value prop and feature cards"
      contains: "See which requirements are verified"
  key_links:
    - from: "Feature highlight cards"
      to: "admin-matrix, admin-flow-status, admin-vendor-coverage, admin-impact-analysis"
      via: "href with Django url tags"
      pattern: "url 'admin-matrix'"
---

<objective>
Enhance landing page with PM-focused value proposition and feature highlight cards

Purpose: Visitors immediately understand what SpecTrace does and can navigate to key features
Output: Updated landing.html with compelling tagline and 4 feature cards linking to dashboard views
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-landing-page/25-RESEARCH.md
@spectrace/templates/admin/requirements/landing.html
@spectrace/templates/admin/requirements/_design_system.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add value proposition and feature highlight cards</name>
  <files>spectrace/templates/admin/requirements/landing.html</files>
  <action>
Update the landing page template with:

1. **Replace tagline** (line 205):
   - Change "Requirements as code, automatically verified" to:
   - Primary: "See which requirements are verified by passing tests"
   - Add subtitle below: "Requirements as code, automatically verified" (keep as secondary line)

2. **Add feature highlights section** after the existing `.landing-paths` div (after line 254):
   - Create new section with 4 feature cards in a grid
   - Use `.feature-highlights` class for the container (4-column grid, 2-column on mobile)
   - Use `.feature-highlight` class for cards (lighter variant of landing-path)

3. **Feature cards to add:**

   a) **Traceability Matrix**
      - Icon: Grid/matrix (4 squares)
      - Title: "Traceability Matrix"
      - Description: "See every requirement and its test coverage at a glance"
      - Link: `{% url 'admin-matrix' %}`

   b) **Verification Flows**
      - Icon: Activity/pulse (polyline zigzag)
      - Title: "Verification Flows"
      - Description: "Multi-step verification pipelines with live status"
      - Link: `{% url 'admin-flow-status' %}`

   c) **Vendor Coverage**
      - Icon: Users (people silhouettes)
      - Title: "Vendor Coverage"
      - Description: "Track pass rates across all your integrations"
      - Link: `{% url 'admin-vendor-coverage' %}`

   d) **Impact Analysis**
      - Icon: Alert circle (circle with exclamation)
      - Title: "Impact Analysis"
      - Description: "Know which tests to run when specs change"
      - Link: `{% url 'admin-impact-analysis' %}`

4. **Add CSS** (in the style block):
   ```css
   .feature-highlights {
       display: grid;
       grid-template-columns: repeat(4, 1fr);
       gap: var(--st-space-4);
       max-width: 900px;
       width: 100%;
       margin-bottom: var(--st-space-10);
   }

   .feature-highlight {
       display: flex;
       flex-direction: column;
       align-items: center;
       padding: var(--st-space-5);
       background: var(--st-surface);
       border: 1px solid var(--st-border);
       border-radius: var(--st-radius-lg);
       text-decoration: none;
       color: inherit;
       transition: all 0.2s ease;
       text-align: center;
   }

   .feature-highlight:hover {
       border-color: var(--st-accent-500);
       transform: translateY(-2px);
   }

   .feature-highlight__icon {
       width: 48px;
       height: 48px;
       display: flex;
       align-items: center;
       justify-content: center;
       background: var(--st-accent-bg);
       border-radius: var(--st-radius-md);
       color: var(--st-accent-500);
       margin-bottom: var(--st-space-3);
   }

   .feature-highlight__title {
       font-size: 1rem;
       font-weight: 600;
       margin-bottom: var(--st-space-1);
   }

   .feature-highlight__desc {
       font-size: 0.8125rem;
       color: var(--st-text-muted);
       line-height: 1.4;
   }

   /* Dark mode for feature highlights */
   html.dark .feature-highlight__title {
       color: #f1f3f5;
   }

   html.dark .feature-highlight__desc {
       color: #adb5bd;
   }

   @media (max-width: 640px) {
       .feature-highlights {
           grid-template-columns: repeat(2, 1fr);
       }
   }
   ```

5. **Icon SVGs** (from research):
   - Matrix: `<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><rect x="3" y="3" width="7" height="7"></rect><rect x="14" y="3" width="7" height="7"></rect><rect x="14" y="14" width="7" height="7"></rect><rect x="3" y="14" width="7" height="7"></rect></svg>`
   - Flows: `<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><polyline points="22 12 18 12 15 21 9 3 6 12 2 12"></polyline></svg>`
   - Vendor: `<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M17 21v-2a4 4 0 0 0-4-4H5a4 4 0 0 0-4 4v2"></path><circle cx="9" cy="7" r="4"></circle><path d="M23 21v-2a4 4 0 0 0-3-3.87"></path><path d="M16 3.13a4 4 0 0 1 0 7.75"></path></svg>`
   - Impact: `<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><circle cx="12" cy="12" r="10"></circle><line x1="12" y1="8" x2="12" y2="12"></line><line x1="12" y1="16" x2="12.01" y2="16"></line></svg>`

6. **Add animation delays** to feature highlights for staggered entrance (st-animate-in, st-animate-delay-N)
  </action>
  <verify>
Run Django dev server and visit http://localhost:8000/:
- New tagline "See which requirements are verified by passing tests" visible
- 4 feature cards visible below existing Dashboard/Tour cards
- Each card has icon, title, and description
- Clicking each card navigates to correct URL
  </verify>
  <done>
- Primary tagline updated to PM-focused value proposition (LAND-01)
- 4 feature highlight cards visible with icons (LAND-02)
- All 4 cards link to correct dashboard views (LAND-03)
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify dark mode rendering</name>
  <what-built>Enhanced landing page with new value proposition and 4 feature highlight cards</what-built>
  <how-to-verify>
1. Start Django dev server: `python manage.py runserver`
2. Visit http://localhost:8000/
3. Verify light mode:
   - Tagline readable
   - Stats section (if data exists) displays correctly
   - All 6 cards (2 paths + 4 features) have visible borders and text
   - Icons have accent background
4. Toggle dark mode (click moon icon in header or use browser dev tools)
5. Verify dark mode:
   - Background switches to dark
   - All text remains readable (no white-on-white or black-on-black)
   - Card borders visible
   - Feature highlight cards have proper contrast
6. Click each of the 4 feature cards to verify navigation works:
   - Traceability Matrix -> /admin/matrix/
   - Verification Flows -> /admin/flow-status/
   - Vendor Coverage -> /admin/vendor-coverage/
   - Impact Analysis -> /admin/impact-analysis/
  </how-to-verify>
  <resume-signal>Type "approved" if landing page looks good in both modes, or describe any issues</resume-signal>
</task>

</tasks>

<verification>
- [ ] Value proposition updated to "See which requirements are verified by passing tests"
- [ ] 4 feature highlight cards rendered below existing navigation cards
- [ ] Each feature card has: icon, title, description, working link
- [ ] Cards use design system variables (--st-surface, --st-border, --st-accent-*)
- [ ] Dark mode text contrast verified (no illegible text)
- [ ] Mobile responsive (2-column grid on small screens)
</verification>

<success_criteria>
1. LAND-01: Landing page displays "See which requirements are verified by passing tests" as primary tagline
2. LAND-02: 4 feature cards visible (Matrix, Flows, Vendor, Impact) each with distinct icon
3. LAND-03: Clicking each feature card navigates to the corresponding admin view
4. LAND-04: User verifies dark mode renders correctly via human checkpoint
</success_criteria>

<output>
After completion, create `.planning/phases/25-landing-page/25-01-SUMMARY.md`
</output>
