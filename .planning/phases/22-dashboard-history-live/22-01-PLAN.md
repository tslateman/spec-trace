---
phase: 22-dashboard-history-live
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - spectrace/requirements/flow_status.py
  - spectrace/requirements/views.py
  - spectrace/templates/admin/requirements/flow_runs.html
  - spectrace/templates/admin/requirements/flow_run_detail.html
autonomous: true

must_haves:
  truths:
    - "User can see list of all flow runs with status, timestamp, duration (HIST-01)"
    - "User can filter runs by status and date range (HIST-02)"
    - "User can drill down to run detail showing step-by-step results (HIST-03)"
    - "User can see step timing and failure messages in detail view (HIST-04)"
  artifacts:
    - path: "spectrace/templates/admin/requirements/flow_runs.html"
      provides: "Flow runs list with filtering"
      min_lines: 150
    - path: "spectrace/templates/admin/requirements/flow_run_detail.html"
      provides: "Flow run detail with step timeline"
      min_lines: 150
  key_links:
    - from: "flow_runs.html"
      to: "flow_run_detail.html"
      via: "url 'admin-flow-run-detail' run.id"
      pattern: "admin-flow-run-detail"
    - from: "views.py flow_runs_view"
      to: "flow_status.py get_flow_runs_data"
      via: "function call with filters"
      pattern: "get_flow_runs_data"
---

<objective>
Create flow runs history list and detail views with filtering capabilities.

Purpose: Provide dashboard views for flow monitoring history (HIST-01 through HIST-04).
Output: Two templates (flow_runs.html, flow_run_detail.html) with filtering support in data layer.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/milestones/v8-ROADMAP.md
@.planning/phases/22-dashboard-history-live/22-RESEARCH.md
@spectrace/requirements/flow_status.py
@spectrace/requirements/views.py
@spectrace/templates/admin/requirements/flow_status.html
@spectrace/templates/admin/requirements/validation_runs.html
@spectrace/templates/admin/requirements/validation_run_detail.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add filtering to flow_runs_view and data layer</name>
  <files>
    spectrace/requirements/flow_status.py
    spectrace/requirements/views.py
  </files>
  <action>
Extend `get_flow_runs_data()` in flow_status.py to accept filters dict:
- `status`: Filter by run status (passed, failed, running)
- `date_from`: Filter runs started on or after this date
- `date_to`: Filter runs started on or before this date

Apply filters to queryset before pagination:
```python
if filters.get('status'):
    queryset = queryset.filter(status=filters['status'])
if filters.get('date_from'):
    queryset = queryset.filter(started_at__date__gte=filters['date_from'])
if filters.get('date_to'):
    queryset = queryset.filter(started_at__date__lte=filters['date_to'])
```

Update `flow_runs_view()` in views.py to:
1. Parse filter params from request.GET (status, date_from, date_to)
2. Build filters dict (use datetime.strptime for dates, same pattern as _build_validation_run_filters)
3. Pass filters to get_flow_runs_data()
4. Add current_filters to context for template
  </action>
  <verify>
Run `python manage.py shell -c "from requirements.flow_status import get_flow_runs_data; print(get_flow_runs_data.__code__.co_varnames)"` shows 'filters' parameter.
  </verify>
  <done>
get_flow_runs_data accepts filters dict with status, date_from, date_to; flow_runs_view parses query params and passes to data layer.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create flow_runs.html template</name>
  <files>
    spectrace/templates/admin/requirements/flow_runs.html
  </files>
  <action>
Create flow runs list template following validation_runs.html pattern:

1. Extend unfold/layouts/base.html, include _design_system.html
2. Breadcrumbs: Dashboard / Flow Status / {flow_name}
3. Header with flow display name and description
4. Summary stats card: Total Runs, Passed, Failed, Pass Rate
5. Filters card with form (method="get"):
   - Status select (All, Passed, Failed, Running)
   - Date From input (type="date")
   - Date To input (type="date")
   - Apply/Clear buttons
6. Runs table with columns:
   - Run ID (link to detail, st-mono)
   - Status (st-badge with pass/fail/warn coloring)
   - Started (date format "M j, H:i")
   - Duration (ms, st-mono)
   - Steps (passed/failed counts with color)
7. Pagination with filter params preserved in links (same pattern as validation_runs.html)
8. Empty state message when no runs found

Use design system classes: st-card, st-table, st-badge, st-btn, st-input, st-select, st-metric.
  </action>
  <verify>
Template syntax check: `python -c "from django.template import Template; Template(open('spectrace/templates/admin/requirements/flow_runs.html').read())"` runs without error.
  </verify>
  <done>
flow_runs.html displays paginated list of flow runs with status/date filters, summary stats, and links to run detail.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create flow_run_detail.html template</name>
  <files>
    spectrace/templates/admin/requirements/flow_run_detail.html
  </files>
  <action>
Create flow run detail template following validation_run_detail.html pattern:

1. Extend unfold/layouts/base.html, include _design_system.html
2. Add flow-pipeline CSS (same as flow_status.html) for step visualization
3. Breadcrumbs: Dashboard / Flow Status / {flow_name} / Run #{id}
4. Header with run ID and status badge, navigation to prev/next runs
5. Summary stats: Total Steps, Passed, Failed, Duration
6. Step pipeline visualization (horizontal bar showing pass/fail for each step)
7. Step timeline (vertical list with expandable details):
   - Each step shows: name, status badge, duration
   - Expandable section (Alpine.js x-show) with:
     - Details text
     - Error message (if failed, red styling)
     - Response status code
     - Response body (collapsible, pre-formatted)
   - Use step-timeline CSS from validation_run_detail.html
8. Run context JSON (collapsible, if present)
9. Back to runs list link

Use Alpine.js for expand/collapse: x-data="{ expanded: false }" pattern.
  </action>
  <verify>
Template syntax check: `python -c "from django.template import Template; Template(open('spectrace/templates/admin/requirements/flow_run_detail.html').read())"` runs without error.
  </verify>
  <done>
flow_run_detail.html shows step pipeline visualization, expandable step timeline with timing and error messages, prev/next navigation.
  </done>
</task>

</tasks>

<verification>
1. Run dev server: `python manage.py runserver`
2. Navigate to /admin/flow-status/, click "Load Demo Data"
3. Click a flow to see runs list at /admin/flow-status/{flow_name}/
4. Verify filters work: apply status filter, apply date filter, clear filters
5. Click a run to see detail at /admin/flow-status/run/{id}/
6. Verify step timeline shows timing and error messages
7. Verify prev/next navigation works between runs
</verification>

<success_criteria>
- HIST-01: Flow runs list shows status, timestamp, duration for all runs
- HIST-02: Status and date range filters work correctly
- HIST-03: Clicking a run shows step-by-step results
- HIST-04: Step detail shows timing (duration_ms) and failure messages (error_message)
- Templates follow design system conventions (st-* classes)
- Pagination preserves filter params
</success_criteria>

<output>
After completion, create `.planning/phases/22-dashboard-history-live/22-01-SUMMARY.md`
</output>
