---
phase: 24-visual-consistency
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - spectrace/templates/admin/requirements/_design_system.html
autonomous: true

must_haves:
  truths:
    - "Tables using .st-table display alternating row colors in light mode"
    - "Tables using .st-table display alternating row colors in dark mode"
    - "Table text is readable in dark mode (light text on dark background)"
  artifacts:
    - path: "spectrace/templates/admin/requirements/_design_system.html"
      provides: "Enhanced .st-table with alternating rows and dark mode text"
      contains: ".st-table tbody tr:nth-child(even)"
  key_links:
    - from: ".st-table tbody tr:nth-child(even)"
      to: "var(--st-surface-sunken)"
      via: "background property"
      pattern: "nth-child\\(even\\).*background.*st-surface-sunken"
    - from: "html.dark .st-table td"
      to: "var(--st-slate-200)"
      via: "color property"
      pattern: "html\\.dark .st-table td.*color.*st-slate-200"
---

<objective>
Enhance the design system `.st-table` class with alternating row colors and explicit dark mode text colors.

Purpose: All 11 tables using `.st-table` automatically get dark-mode-aware alternating rows without any template changes.
Output: Updated `_design_system.html` with enhanced `.st-table` styles.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-visual-consistency/24-RESEARCH.md

# Reference pattern from qa_ecosystem.html (lines 43-77 in research)
# Current .st-table implementation (lines 549-579 in _design_system.html)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add alternating rows and dark mode text to .st-table</name>
  <files>spectrace/templates/admin/requirements/_design_system.html</files>
  <action>
In the DATA TABLE section (around line 546-580), update the `.st-table` styles:

1. Add base background to `.st-table tbody tr`:
   ```css
   .st-table tbody tr {
       background: var(--st-surface);
       transition: background 0.1s var(--st-ease-out);
   }
   ```

2. Add alternating row pattern BEFORE the hover rule:
   ```css
   .st-table tbody tr:nth-child(even) {
       background: var(--st-surface-sunken);
   }
   ```

3. Keep the existing hover rule (it already uses `var(--st-surface-sunken)`)

4. Add dark mode text color override AFTER the hover rule:
   ```css
   html.dark .st-table td {
       color: var(--st-slate-200);
   }
   ```

The final DATA TABLE section should have this structure:
- .st-table (width, border-collapse)
- .st-table th (header styles)
- .st-table td (cell styles)
- .st-table tbody tr (base background + transition)
- .st-table tbody tr:nth-child(even) (alternating)
- .st-table tbody tr:hover (hover state)
- html.dark .st-table td (dark mode text)
  </action>
  <verify>
1. Open any page with `.st-table` (e.g., validation_runs.html)
2. Verify alternating rows visible in light mode
3. Toggle dark mode
4. Verify alternating rows visible in dark mode
5. Verify text is readable (light gray, not dark gray)
  </verify>
  <done>
- .st-table has alternating row colors using var(--st-surface) and var(--st-surface-sunken)
- Dark mode explicitly sets td color to var(--st-slate-200)
- All 11 existing .st-table users automatically get the enhancement
  </done>
</task>

</tasks>

<verification>
Run the dev server and check a page with .st-table:
```bash
python manage.py runserver
```

Visit http://localhost:8000/admin/requirements/validation-runs/ and verify:
1. Rows alternate between white and light gray in light mode
2. Rows alternate between dark grays in dark mode
3. Text is readable in both modes
</verification>

<success_criteria>
1. [VIS-04 partial] .st-table has alternating row pattern
2. Tables using .st-table display correctly in dark mode
3. No visual regressions in light mode
</success_criteria>

<output>
After completion, create `.planning/phases/24-visual-consistency/24-01-SUMMARY.md`
</output>
