---
phase: 26-demo-data-hub
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - demos.yaml
  - scripts/list_demos.py
  - specs/sample/SAMPLE-001-platform.md
  - specs/sample/feature-auth/SAMPLE-AUTH-001.md
  - specs/sample/feature-auth/stories/SAMPLE-AUTH-001-001.md
  - specs/sample/feature-auth/stories/SAMPLE-AUTH-001-002.md
  - specs/sample/feature-api/SAMPLE-API-001.md
  - specs/sample/feature-api/stories/SAMPLE-API-001-001.md
  - specs/sample/feature-api/stories/SAMPLE-API-001-002.md
autonomous: true

must_haves:
  truths:
    - "demos.yaml has no unused fields (options removed)"
    - "list_demos.py handles demos without options field"
    - "Sample specs exist with 3+ level hierarchy (epic -> feature -> story)"
    - "parse_specs successfully imports sample hierarchy"
  artifacts:
    - path: "demos.yaml"
      provides: "Demo catalog without vestigial options field"
      contains: "- id: document-pipeline"
    - path: "specs/sample/SAMPLE-001-platform.md"
      provides: "Root epic (depth=1)"
      contains: "id: SAMPLE-001"
    - path: "specs/sample/feature-auth/SAMPLE-AUTH-001.md"
      provides: "Feature (depth=2, parent: SAMPLE-001)"
      contains: "parent: SAMPLE-001"
    - path: "specs/sample/feature-auth/stories/SAMPLE-AUTH-001-001.md"
      provides: "Story (depth=3, parent: SAMPLE-AUTH-001)"
      contains: "parent: SAMPLE-AUTH-001"
  key_links:
    - from: "specs/sample/feature-auth/SAMPLE-AUTH-001.md"
      to: "specs/sample/SAMPLE-001-platform.md"
      via: "parent field in frontmatter"
      pattern: "parent: SAMPLE-001"
    - from: "specs/sample/feature-auth/stories/SAMPLE-AUTH-001-001.md"
      to: "specs/sample/feature-auth/SAMPLE-AUTH-001.md"
      via: "parent field in frontmatter"
      pattern: "parent: SAMPLE-AUTH-001"
---

<objective>
Clean demos.yaml of unused fields and create sample specs with realistic 3-level requirement hierarchy.

Purpose: DEMO-01 removes vestigial YAML fields; DEMO-02 provides sample data showing epic -> feature -> story pattern for demo navigation.
Output: Clean demos.yaml, sample spec files with proper parent relationships.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-demo-data-hub/26-RESEARCH.md

# Key existing files
@demos.yaml
@scripts/list_demos.py
@specs/auth/login.md (example of existing spec format)
@examples/document-pipeline/specs/pipeline/ingest/DOC-ING-002.md (example of depth=3 spec)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove unused options field from demos.yaml and list_demos.py</name>
  <files>demos.yaml, scripts/list_demos.py</files>
  <action>
1. In demos.yaml, remove lines 95-96 (the `options:` field and its `--skip-tests` value under document-pipeline entry).

2. In scripts/list_demos.py, remove lines 71-74 that handle the options field:
   - Remove the `if demo.get("options"):` block
   - Remove the `print("\nOptions:")` line
   - Remove the `for opt, desc in demo["options"].items():` loop

Both files have vestigial code - options was likely intended for CLI flags but is unused.
  </action>
  <verify>
- `grep -n "options" demos.yaml` returns no matches
- `grep -n "options" scripts/list_demos.py` returns no matches
- `python scripts/list_demos.py --show document-pipeline` runs without error
  </verify>
  <done>
- demos.yaml has no options field
- list_demos.py has no options handling code
- All demos still display correctly via list_demos.py
  </done>
</task>

<task type="auto">
  <name>Task 2: Create sample specs with 3-level hierarchy</name>
  <files>
specs/sample/SAMPLE-001-platform.md
specs/sample/feature-auth/SAMPLE-AUTH-001.md
specs/sample/feature-auth/stories/SAMPLE-AUTH-001-001.md
specs/sample/feature-auth/stories/SAMPLE-AUTH-001-002.md
specs/sample/feature-api/SAMPLE-API-001.md
specs/sample/feature-api/stories/SAMPLE-API-001-001.md
specs/sample/feature-api/stories/SAMPLE-API-001-002.md
  </files>
  <action>
Create 7 sample spec files with 3-level hierarchy:

**Level 1 - Epic (depth=1):**
```markdown
# specs/sample/SAMPLE-001-platform.md
---
id: SAMPLE-001
title: Sample Platform Services
tags: [epic, platform, sample]
priority: high
status: active
verification_method: both
---

Sample epic for demonstrating requirement hierarchy. This represents a top-level business capability.

## Overview

The Sample Platform demonstrates SpecTrace's ability to track requirements at multiple levels of granularity - from high-level epics down to specific user stories.

## Acceptance Criteria

- All child features are properly linked
- Requirements can be navigated via parent-child relationships
- Verification status rolls up from stories to features to epics
```

**Level 2 - Features (depth=2, parent: SAMPLE-001):**

Create SAMPLE-AUTH-001 (Authentication Feature) and SAMPLE-API-001 (API Feature), each with:
- `parent: SAMPLE-001` in frontmatter
- Tags including `feature`
- 3-5 acceptance criteria

**Level 3 - Stories (depth=3):**

Create 2 stories under each feature:
- SAMPLE-AUTH-001-001: User Login Story (parent: SAMPLE-AUTH-001)
- SAMPLE-AUTH-001-002: Password Reset Story (parent: SAMPLE-AUTH-001)
- SAMPLE-API-001-001: Create Resource Story (parent: SAMPLE-API-001)
- SAMPLE-API-001-002: List Resources Story (parent: SAMPLE-API-001)

Each story should have:
- `parent:` pointing to its feature
- Tags including `story`
- Specific, testable acceptance criteria (3-4 items)
- `verification_method: test`

Use realistic content that mirrors actual development scenarios. Reference the existing spec format in specs/auth/login.md for style.
  </action>
  <verify>
- `ls -la specs/sample/` shows sample directory structure
- `find specs/sample -name "*.md" | wc -l` returns 7
- Each file has valid YAML frontmatter (grep for "^---")
- `python manage.py parse_specs specs/sample/ --dry-run` shows 7 requirements with correct parent relationships
  </verify>
  <done>
- 7 sample spec files exist with proper hierarchy
- SAMPLE-001 has no parent (epic)
- SAMPLE-AUTH-001 and SAMPLE-API-001 have parent: SAMPLE-001 (features)
- 4 story files have parents pointing to their respective features
- All files follow existing spec format conventions
  </done>
</task>

</tasks>

<verification>
1. Run list_demos.py to confirm all demos still display:
   ```bash
   python scripts/list_demos.py
   python scripts/list_demos.py --show document-pipeline
   ```

2. Parse sample specs and verify hierarchy:
   ```bash
   python manage.py parse_specs specs/sample/
   ```

3. Query requirements to verify depth:
   ```bash
   python manage.py shell -c "from requirements.models import Requirement; print([(r.external_id, r.depth) for r in Requirement.objects.filter(external_id__startswith='SAMPLE')])"
   ```
   Should show: SAMPLE-001 at depth=1, features at depth=2, stories at depth=3.
</verification>

<success_criteria>
- demos.yaml contains no `options` field
- scripts/list_demos.py contains no options handling
- 7 sample spec files exist in specs/sample/ hierarchy
- Requirements import with correct parent-child relationships
- Requirements show 3 distinct depth levels (1, 2, 3)
</success_criteria>

<output>
After completion, create `.planning/phases/26-demo-data-hub/26-01-SUMMARY.md`
</output>
